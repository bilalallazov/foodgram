# Ваши данные — куда вставить (копируйте по таблице)

## GitHub → Settings → Secrets → Actions

Создайте **5** секретов (New repository secret):

### 1. HOST
```
81.26.183.233
```

### 2. USER
```
yc-user
```
Если не подойдёт при деплое — попробуйте `ubuntu`.

### 3. SSH_PRIVATE_KEY
Весь текст из файла `C:\Users\user\.ssh\foodgram_vm`  
(от `-----BEGIN OPENSSH PRIVATE KEY-----` до `-----END...`)

### 4. YC_REGISTRY_ID
ID из Yandex Cloud → Container Registry (строка `crp...`).

### 5. YC_SA_JSON_CREDENTIALS
Весь JSON из IAM → сервисный аккаунт → авторизованный ключ.

---

## Файл tests.yml (уже в проекте, только push)

```yaml
login: admin@foodgram.ru
password: admin123
vm_name: r-backend-vm-1670829377
```

---

## После добавления секретов

```powershell
cd "C:\Users\user\Desktop\Новая папка (2)\foodgram"
git add .
git commit -m "Add deploy workflow and docs"
git push
```

1. Actions → **Foodgram CI** — зелёный  
2. Actions → **Foodgram Deploy** — зелёный  
3. Браузер → http://81.26.183.233  
4. Практикум → Отправить на ревью

---

## SSH-ключ на ВМ

В консоли Yandex на ВМ должен быть публичный ключ:

```powershell
Get-Content C:\Users\user\.ssh\foodgram_vm.pub
```

Вставить в ВМ → Изменить → SSH-ключи.
